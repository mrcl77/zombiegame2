[package]
name = "zombiegame2"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.14"
rand = "0.8"
serde = { version = "1", features = ["derive"] }
bincode = "1.3"
serde_json = "1"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
